include ../../review/review
.profile-reviews__reviewContainer
    if reviewsList
        each el, _ in reviewsList 
            +review(el)
    else 
        .profile-reviews__reviewContainer__empty-container
            p.empty-container__text Список отзывов пуст
