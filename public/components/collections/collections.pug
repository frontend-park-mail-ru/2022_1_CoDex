include ../collection/collection

div.content_wrapper
    page.page
        div(class = "content_container")
            h1.title Подборки
            div.description Коллекции фильмов на любой вкус
            div.bg_container
                div.collections__container
                    each input, i in collectionlist 
                            +collection(input)
